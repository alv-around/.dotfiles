# Cheatsheet

## Zsh

### Useful commands

- `$ search` - search for a string in the subdirectories of current dir
- `alt + c` - search subfolder below the current dir
- `ctrl + t` - search for file in current dir or subdirs
